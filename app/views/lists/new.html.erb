<h1>New List</h1>

<%= form_with(model: @list, url: lists_path) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <!-- Other fields for list creation -->

  <%= form.submit "Create List" %>
<% end %>
